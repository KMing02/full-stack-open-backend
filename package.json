{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "backend for the phonebook app",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd /Users/linkeming/Documents/GitHub/full-stack-open/part2/phoneBook/phonebook && npm run build && cp -r build /Users/linkeming/Documents/GitHub/full-stack-open-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1",
    "morgan": "^1.10.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
